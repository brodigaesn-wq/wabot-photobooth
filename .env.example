# Copy file ini jadi ".env" (tanpa ".example") lalu isi API key asli kamu.
# File ".env" TIDAK boleh di-commit ke Git (sudah otomatis di-skip via .gitignore).

GOOGLE_API_KEY=isi_dengan_api_key_google_drive_kamu
